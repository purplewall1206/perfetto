# Prevent system pnpm (v9/v10) from modifying the lockfile.
# Perfetto uses a hermetic bundled pnpm (tools/pnpm).
# If you need to add/remove deps, use:  tools/pnpm add <pkg> --dir ui
frozen-lockfile=true
